info:
  name: Get All Recipes
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/recipes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
