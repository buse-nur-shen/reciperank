info:
  name: Recipe 1
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/api/recipes"
  body:
    type: json
    data: |-
      {
        "title": "Chocolate Cake",
        "category": "baking",
        "subcategory": "cakes",
        "ingredients": ["flour", "butter", "sugar", "cocoa powder", "eggs"],
        "instructions": "Mix ingredients and bake at 180C for 30 minutes.",
        "cookingTime": 30,
        "difficulty": "medium",
        "servings": 8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
