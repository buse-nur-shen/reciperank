info:
  name: Register User
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/users/register"
  body:
    type: json
    data: |
      {
        "username": "testuser",
        "email": "test@test.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
