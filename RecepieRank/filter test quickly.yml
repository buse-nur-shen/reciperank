info:
  name: " filter test quickly"
  type: http
  seq: 15

http:
  method: GET
  url: "{{baseUrl}}/api/recipes?category=baking"
  params:
    - name: category
      value: baking
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
