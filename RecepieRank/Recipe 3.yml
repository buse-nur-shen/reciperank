info:
  name: Recipe 3
  type: http
  seq: 18

http:
  method: POST
  url: "{{baseUrl}}/api/recipes"
  body:
    type: json
    data: |-
      {
        "title": "Sourdough Bread",
        "category": "baking",
        "subcategory": "breads",
        "ingredients": ["flour", "water", "salt", "sourdough starter"],
        "instructions": "Mix ingredients, let rise overnight, bake at 220C for 40 minutes.",
        "cookingTime": 60,
        "difficulty": "hard",
        "servings": 6
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
