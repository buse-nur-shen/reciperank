info:
  name: " Filter by difficulty"
  type: http
  seq: 19

http:
  method: GET
  url: "{{baseUrl}}/api/recipes?difficulty=hard"
  params:
    - name: difficulty
      value: hard
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
